Релиз "{{ .Release.Name }}" установлен в namespace "{{ .Release.Namespace }}".

Версия frontend (nginx): {{ .Values.frontend.image.tag }}
Версия backend (multitool): {{ .Values.backend.image.tag }}

Проверить статус подов:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Проверить доступность frontend (port-forward):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "myapp.fullname" . }}-frontend 8080:{{ .Values.frontend.service.port }}
  curl -I http://localhost:8080/
